@using Microsoft.AspNetCore.Identity
@model IdentityRole
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<br />

<h2 class="text-danger">Delete Role @Model.Name </h2>

<form asp-action="Delete" method="post" enctype="multipart/form-data" id="role-form">
    <div class="p-4 rounded border">
        <div asp-validation-summary="All" class="text-danger">
        </div>
        <h3 class="text-danger">@ViewBag.message</h3>
        <h4>Do you want delete this role</h4>
        <div class="form-group row">
            <div class="col-2">
                <label asp-for="Name"></label>
            </div>
            <div class="col-5">
                <input asp-for="Name" disabled id="name" class="form-control" />
                <input value="@Model.Id" type="hidden" />
            </div>
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <br />

        <div>
            <input type="submit" class="btn btn-danger" value="Delete" />
            <a asp-action="Edit" class="btn btn-outline-success">Update</a>
            <a asp-action="Index" class="btn btn-success">Back To List</a>
        </div>
    </div>
</form>

@section Scripts {

    <partial name="_ValidationScriptsPartial" />



    <script>
        $(document).ready(function () {
            $("#role-form").validate({
                rules: {
                    UserName: {
                        required: true
                    }
                }
            });
        });
    </script>
}